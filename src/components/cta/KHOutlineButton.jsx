export const KHOutlineButton = ({children}) => {
    return (
        <button
            className="text-sm md:text-base py-1 px-6 md:py-2 rounded-md text-info font-light tracking-wide bg-smoke">
            {children}
        </button>
    )
}