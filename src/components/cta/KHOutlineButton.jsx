export const KHOutlineButton = ({children}) => {
    return (
        <button
            className="px-6 py-2 rounded-md text-info font-semibold tracking-wide bg-smoke">
            {children}
        </button>
    )
}